{;; `resources` carries the vendored operator-console stylesheet
 ;; (resources/foodmachmfg/console.css). It is checked in rather than
 ;; resolved as a dependency so `clojure -M:dev:render-html` emits the
 ;; same bytes with no network and no populated ~/.gitlibs -- see
 ;; dev/vendor_console_css.clj for how it is regenerated.
 :paths ["src" "resources"]
 ;; langgraph brings langchain transitively (both zero-/single-dep,
 ;; all .cljc -> portable to JVM / SCI / ClojureScript / GraalVM). Both
 ;; live in the kotoba-lang org of this workspace. This repo is
 ;; standalone (forkable outside the workspace); the :local/root paths
 ;; only resolve inside the monorepo checkout -- a standalone fork
 ;; should override them with git coordinates. langchain is pinned here
 ;; directly (not only under :dev) so a bare `clojure -M:test` works
 ;; offline inside the monorepo checkout without depending on
 ;; langgraph's own transitive git/sha pin being fetchable.
 :deps {io.github.kotoba-lang/langgraph
        {:local/root "../../kotoba-lang/langgraph"}
        io.github.kotoba-lang/langchain
        {:local/root "../../kotoba-lang/langchain"}}
 :aliases
 {;; offline workspace dev -- pin transitive langchain to local checkout
  ;; (kept for parity with sibling actors' deps.edn shape; redundant
  ;; with the top-level :deps override above).
  :dev {:override-deps
        {io.github.kotoba-lang/langchain
         {:local/root "../../kotoba-lang/langchain"}}}
  ;; clojure -M:dev:run  -> drive the demo through one OperationActor
  :run {:main-opts ["-m" "foodmachmfg.sim"]}
  ;; clojure -M:dev:render-html [out-file] -> regenerate
  ;; docs/samples/operator-console.html by running the REAL actor
  ;; (flagship checklist item 2). Fails rather than writes if the
  ;; scenario stops producing HARD governor holds.
  :render-html {:main-opts ["-m" "foodmachmfg.render-html"]}
  ;; clojure -M:vendor-css -> refresh resources/foodmachmfg/console.css
  ;; from the sibling jp-go-dds checkout. NOT needed to build the
  ;; console: the normal render reads the checked-in resource, so it
  ;; stays offline. Only run when picking up an upstream skin change.
  :vendor-css {:extra-paths ["dev"]
               :extra-deps
               {io.github.kotoba-lang/jp-go-digital-design-system
                {:local/root "../../kotoba-lang/jp-go-digital-design-system"}}
               :main-opts ["-m" "vendor-console-css"]}
  :test {:extra-paths ["test"]
         :extra-deps {io.github.cognitect-labs/test-runner
                      {:git/tag "v0.5.1" :git/sha "dfb30dd"}}
         :main-opts ["-m" "cognitect.test-runner"]
         :exec-fn cognitect.test-runner.api/test}
  ;; static analysis -- clj-kondo from deps (no binary), errors fail CI.
  :lint {:replace-deps {clj-kondo/clj-kondo {:mvn/version "2024.11.14"}}
         :main-opts    ["-m" "clj-kondo.main"
                        "--lint" "src" "test"
                        "--fail-level" "error"]}}}
